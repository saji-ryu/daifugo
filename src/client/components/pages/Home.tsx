import React from "react";
import styled from "@emotion/styled";

type Props = {};

const Home = (props: Props) => <Wrapper>{"home"}</Wrapper>;

const Wrapper = styled("div")({
  color: "red",
});

export default Home;
